services:
  application:
    environment:
      - APP_URL=${APP_URL}
      - CONSOLE_HOST=${CONSOLE_HOST}
      - API_HOST=${API_HOST}
      - API_NAMESPACE=${API_NAMESPACE}
      - API_SECURE=${API_SECURE}